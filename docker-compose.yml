version: '3.8'

services:
  db:
    image: nouchka/sqlite3
    container_name: sqlite_db
    volumes:
      - ./data:/data

  app:
    build: .
    container_name: python_app
    depends_on:
      - db
    volumes:
      - ./data:/data

    
